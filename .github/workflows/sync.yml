name: Sync with sunnypilot upstream

on:
  workflow_dispatch:
  schedule:
    # optional: daily at 03:00 UTC — remove or adjust as desired
    - cron: '0 3 * * *'

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          repository: 'ACTS-HORIZON/openpilot'
          submodules: 'recursive'
          ssh-key: ${{ secrets.SSH_KEY }}
          
      - name: Configure git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Pull Submodules
        continue-on-error: true
        run: |
          git submodule init
          git pull --recurse

      - name: Pull upstream
        continue-on-error: true
        run: |
          git remote add upstream https://github.com/sunnypilot/sunnypilot.git
          git fetch upstream

      - name: Final Touches
        run: |
          git checkout master
          git add opendbc_repo

      - name: Merge and Push
        run: |
          git merge upstream/master
          git push
